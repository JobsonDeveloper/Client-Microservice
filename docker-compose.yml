name: e-sale

services:
  client:
    build: .
    ports:
      - "8082:8082"
    networks:
      - ecommerce-network
networks:
  ecommerce-network:
    name: ecommerce-network
    driver: bridge
    external: true